# Google Maps API Key
# 1. Go to https://console.cloud.google.com/google/maps-apis/
# 2. Create a new project (or use an existing one)
# 3. Enable the "Maps JavaScript API"
# 4. Create an API key under "Credentials"
# 5. Restrict the key to your deployed Vercel domain for security
# 6. Replace YOUR_GOOGLE_MAPS_API_KEY in index.html with your actual key
#
# Note: The Google Maps key lives in index.html (frontend), not here.
# This file is a reference for any future server-side keys you add.

GOOGLE_MAPS_API_KEY=your_key_here
